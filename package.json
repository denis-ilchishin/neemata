{
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=7.3.0 <8"
  },
  "os": [
    "darwin",
    "linux"
  ],
  "packageManager": "pnpm@7.3.0",
  "devDependencies": {
    "eslint": "^8.17.0",
    "semver": "^7.3.8",
    "tsup": "^6.5.0",
    "typescript": "^4.7.0"
  },
  "scripts": {
    "version": "node scripts/version.js",
    "pack": "node scripts/pack.js",
    "build": "pnpm -r run build",
    "watch": "pnpm -r --parallel run watch",
    "publish": "pnpm -r build && pnpm -r publish --no-git-checks --access public"
  },
  "version": "0.7.3"
}